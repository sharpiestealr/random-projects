def calcula_media(alunos):
    soma = int([alunos])
    count = 0
    while (count != len(alunos)-1):
        sum = soma[count]+soma[count+1]
        count +=1
    med = sum/len(alunos)
    return med

#não funciona pra lista????
